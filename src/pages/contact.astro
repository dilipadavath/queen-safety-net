---
import BaseLayout from "@/layouts/BaseLayout.astro";
import ContactPage from "@/views/Contact";
import { getBreadcrumbSchema, getPageSchema, SITE_URL } from "@/lib/seo";

const title = "Contact Queen Safety Nets Chennai | Free Site Visit & Quote";
const description =
  "Contact Queen Safety Nets for balcony nets, pigeon nets, bird spikes and sports nets in Chennai. Call +91 7550022405 for free site visit.";
const path = "/contact/";
---

<BaseLayout
  title={title}
  description={description}
  canonicalPath={path}
  schema={[
    getPageSchema(`${SITE_URL}${path}`, title, description),
    getBreadcrumbSchema([
      { name: "Home", path: "/" },
      { name: "Contact", path },
    ]),
  ]}
>
  <ContactPage client:load />
</BaseLayout>
