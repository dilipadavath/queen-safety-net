---
import BaseLayout from "@/layouts/BaseLayout.astro";
import IndexPage from "@/views/Index";
import { getBreadcrumbSchema, getPageSchema, SITE_URL } from "@/lib/seo";

const title =
  "Safety Nets In Chennai | Balcony, Pigeon, Bird & Sports Nets - Queen Safety Nets";
const description =
  "Queen Safety Nets provides balcony safety nets, pigeon nets, bird protection and sports nets installation across Chennai with free site visit and quick quote.";
const path = "/";
---

<BaseLayout
  title={title}
  description={description}
  canonicalPath={path}
  schema={[
    getPageSchema(`${SITE_URL}${path}`, title, description),
    getBreadcrumbSchema([{ name: "Home", path: "/" }]),
  ]}
>
  <IndexPage client:load />
</BaseLayout>
